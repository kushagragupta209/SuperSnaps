# Core
Flask==3.0.3
requests==2.32.3
beautifulsoup4==4.12.3

# Postgres support — only actually imported when DATABASE_URL is set
# (see db_pg.py); local SQLite mode (the default) doesn't need this, but
# it's listed unconditionally so `pip install -r requirements.txt` works
# for either mode without a second install step.
psycopg2-binary==2.9.9

# Production WSGI server — used on Render (see render.yaml); not needed
# for local `python app.py`, which uses Flask's built-in dev server.
gunicorn==22.0.0